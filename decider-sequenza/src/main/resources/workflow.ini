# Specifies a relative output path (for example, a unique folder name) AFTER the output_prefix
output_dir=seqware-results
# Specifies the absolute path of the final destination of the files
# Pick a path that is available on all cluster nodes and can be written by your user
output_prefix=./

build_indexes=true
sample_rate=1000
normal_insert_max=1500
map_qual_cut=5
max_qual_cutoff=30

